// const { countdown } = require("./recursion/countdown"); // recursion

const check = () => "JestTest";

const main = () => {
  console.log("Happy Coding!");
  // countdown(11292); // recursion (higher than 11292 will cause a stack overflow)
};

main();

module.exports = {
  check,
};
